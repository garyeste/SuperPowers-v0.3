cmake_minimum_required(VERSION 3.25.0)

project(RockTrigger
    VERSION 0.2.0.0
    DESCRIPTION "VST3 audio-triggered sample replacer"
)

include(FetchContent)

# Keep the SDK build focused, but enable VSTGUI for our custom editor.
set(SMTG_ENABLE_VST3_PLUGIN_EXAMPLES OFF CACHE BOOL "" FORCE)
set(SMTG_ENABLE_VST3_HOSTING_EXAMPLES OFF CACHE BOOL "" FORCE)
set(SMTG_ENABLE_VSTGUI_SUPPORT ON CACHE BOOL "" FORCE)
set(SMTG_CREATE_PLUGIN_LINK OFF CACHE BOOL "" FORCE)

FetchContent_Declare(
    vst3sdk
    GIT_REPOSITORY https://github.com/steinbergmedia/vst3sdk.git
    GIT_TAG master
    GIT_SHALLOW TRUE
    GIT_SUBMODULES_RECURSE TRUE
)
FetchContent_MakeAvailable(vst3sdk)

smtg_enable_vst3_sdk()

smtg_add_vst3plugin(RockTrigger
    source/rocktriggerids.h
    source/rocktriggerprocessor.h
    source/rocktriggerprocessor.cpp
    source/rocktriggercontroller.h
    source/rocktriggercontroller.cpp
    source/rocktriggerentry.cpp
    source/samplebank.h
    source/samplebank.cpp
)

target_link_libraries(RockTrigger PRIVATE sdk vstgui_support)
target_compile_features(RockTrigger PRIVATE cxx_std_17)

smtg_target_add_plugin_resources(RockTrigger
    RESOURCES
        resource/rocktrigger.uidesc
)

if(SMTG_WIN)
    target_link_libraries(RockTrigger PRIVATE shell32 ole32)
endif()

smtg_target_configure_version_file(RockTrigger)
